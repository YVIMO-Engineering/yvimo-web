import test from 'node:test';import assert from 'node:assert/strict';import {calculateImportEstimate,type ImportEstimateInput} from './importCosting.ts';
const input:ImportEstimateInput={clientName:'Magna',supplier:'GFIL',partNumber:'17864-T4',description:'Shaving Cutter',quantity:40,country:'Brazil',operationType:'Purchase',purchaseCurrency:'USD',invoiceUnitValue:1380,purchaseFx:17.4,saleCurrency:'USD',clientUnitPrice:1950,saleFx:17.4,desiredMarginPercent:5,internationalFreight:23833.476,insurance:0,taxes:150000,customsAgentFees:8600,handling:15600,domesticTransport:5600,otherExpenses:0,logisticsManagement:0};
test('matches workbook core calculation',()=>{const result=calculateImportEstimate(input);assert.equal(result.merchandiseCost,960480);assert.equal(result.logisticsCost,203633.476);assert.equal(result.totalCost,1164113.476);assert.equal(result.clientSale,1357200);assert.ok(Math.abs(result.minimumUnitPrice-1672.576833333333)<.0001);assert.equal(result.result,'PROFIT')});
test('distinguishes break-even from profit',()=>{const base=calculateImportEstimate(input);const result=calculateImportEstimate({...input,clientUnitPrice:base.totalCost/input.quantity/input.saleFx});assert.equal(result.result,'BREAK-EVEN')});
