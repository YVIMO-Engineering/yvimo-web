import test from 'node:test'; import assert from 'node:assert/strict'; import { calculateQuotationTotals, legacyRecurringItem, type QuotationItem } from './quotationV1.ts';
const row=(category:QuotationItem['category'],subtotal:number,extra:Partial<QuotationItem>={}):QuotationItem=>({id:String(Math.random()),category,pricingType:'fixed',name:'x',description:'x',quantity:1,unit:'service',hours:0,unitPrice:subtotal,hourlyRate:0,internalCost:0,subtotal,sortOrder:0,isOptional:false,isSelected:true,isRecurring:false,isCustomerVisible:true,notes:'',...extra});
test('first time totals and recurring addon future price',()=>{const t=calculateQuotationTotals([row('one_time_engineering',300),row('recurring_service',200),row('addon',25,{isRecurring:true})],100,0);assert.equal(t.quotationTotal,625);assert.equal(t.estimatedFutureRepeatPrice,325)});
test('existing program excludes engineering',()=>assert.equal(calculateQuotationTotals([row('recurring_service',200)],100,0).quotationTotal,300));
test('optional item only counts when selected',()=>{const base=[row('recurring_service',200),row('coating',100),row('addon',30,{isOptional:true,isSelected:false})];assert.equal(calculateQuotationTotals(base,100,0).quotationTotal,300);base[2].isSelected=true;assert.equal(calculateQuotationTotals(base,100,0).quotationTotal,330)});
test('program modification excludes engineering from repeat',()=>{const t=calculateQuotationTotals([row('one_time_engineering',120),row('recurring_service',200)],100,0);assert.equal(t.quotationTotal,420);assert.equal(t.estimatedFutureRepeatPrice,300)});
test('legacy machine minutes convert without changing machine price',()=>{const legacy=legacyRecurringItem(120,65);const t=calculateQuotationTotals([legacy],75.45,0);assert.equal(t.recurringServiceSubtotal,130);assert.equal(t.quotationTotal,205.45)});
